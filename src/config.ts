// GitHub OAuth App client ID for device flow.
// Create an OAuth App at https://github.com/settings/developers
// Authorization callback URL can be any valid URL (device flow doesn't use redirects).
// Homepage URL should match your deployment (e.g. https://tinydarkforge.github.io/IntakePad/).
export const GITHUB_CLIENT_ID = "your_client_id_here"
